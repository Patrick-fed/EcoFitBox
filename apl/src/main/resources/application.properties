spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}

spring.application.name=ecofit-apl

jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}

server.port=${SERVER_PORT:65009}

payment.encryption.key=${PAYMENT_ENCRYPTION_KEY}

server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true
server.servlet.encoding.force-response=true

spring.datasource.hikari.connection-init-sql=SET client_encoding TO 'UTF8'

# firebase.service-account is loaded directly from classpath in FirebaseConfig.java
